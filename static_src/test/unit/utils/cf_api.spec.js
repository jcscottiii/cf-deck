
import '../../global_setup.js';

xdescribe('cfApi', () => {
  describe('fetchOrg', () => {
    it('should make http call for org summary', (done) => {
      var spy = sinon.spy(http
      var testRes = {
        guid: '12s',
        name: 'testA',
        spaces: []
      };

    });
  });
});
